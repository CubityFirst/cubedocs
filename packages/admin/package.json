{
  "name": "@annex/admin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && pnpm dev\" \"pnpm exec wrangler dev --port 8789 --persist-to ../../.wrangler/state\"",
    "build": "cd frontend && pnpm build",
    "deploy": "pnpm build && pnpm exec wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "concurrently": "^9.2.1",
    "typescript": "^5.5.0",
    "vitest": "^2.1.9"
  }
}
