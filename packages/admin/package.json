{
  "name": "@cubedocs/admin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd frontend && pnpm build && cd .. && pnpm exec wrangler dev --port 8789 --persist-to ../../.wrangler/state",
    "build": "cd frontend && pnpm build",
    "deploy": "pnpm build && pnpm exec wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
