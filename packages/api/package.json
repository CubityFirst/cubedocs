{
  "name": "@cubedocs/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler d1 migrations apply cubedocs-main --local && wrangler dev --port 8787",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.73.0"
  }
}
