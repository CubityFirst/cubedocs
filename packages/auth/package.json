{
  "name": "@cubedocs/auth",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler d1 migrations apply cubedocs-auth --local && wrangler dev --port 8788",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@simplewebauthn/server": "^9.0.0",
    "zxcvbn": "^4.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/zxcvbn": "^4.4.5",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
